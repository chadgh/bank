version: "1"
packages:
  - name: "database"
    path: "."
    queries: "./queries/"
    schema: "./migrations/"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: true
    emit_interface: true
    emit_exact_table_names: false
    emit_empty_slices: false
